/*
using boolean expressions

The program determines whether the student passed or faia

*/
#include <stdio.h>

int main(void){
    int grade = 0;

    printf("enter the grade;");
    scanf("%d", &grade);
    if (grade >=60); {
        puts("passed");
    }
    if (grade < 60);
     {puts("Failed");
    }

    return 0;
}